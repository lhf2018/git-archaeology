# 技术方案：git-archaeology（`git-arch`）

| 项目 | 内容 |
|---|---|
| 对应 PRD | v1.0 (MVP) |
| 文档版本 | v1.0 |
| 实现语言 | **Python 3.10+** |
| 交付形态 | `pip install` 可执行 CLI；报告输出 **Markdown + HTML**（另支持 JSON/SARIF） |
| 范围 | M1（F1–F6）详细设计；M2/M3 接口预留 |

---

## 1. 设计目标与约束

### 1.1 目标
- 在本地/CI 对 Git 仓库做历史指标采样 → 变点检测 → 规则归因 → 可行动报告。
- 首次分析 ≤ 5 分钟（约 10k commits）；缓存命中后 ≤ 1 分钟。
- 每个拐点可追溯到具体 commit 与指标依据；作者默认脱敏。

### 1.2 约束（与 PRD 对齐）
- 不做 Web UI、PR 插件、跨仓库、自动修复、个人绩效排行。
- 默认离线、无遥测。
- 报告：**Markdown 与 HTML 同为 M1 一等格式**（JSON 供机器；SARIF 放 M2）。

### 1.3 技术选型结论

| 决策 | 选择 | 理由 |
|---|---|---|
| 语言 | Python 3.10+ | `ruptures`(PELT)、`lizard`、GitPython/`subprocess` 生态成熟 |
| Git 访问 | `subprocess` 调 `git` CLI（主）+ 少量 GitPython | 行为与用户本机 Git 一致，易调试；避免纯库语义偏差 |
| AST/复杂度 | lizard（主）+ tree-sitter（扩展预留） | MVP 快速覆盖 Py/JS/TS/Java；后续按语言换 tree-sitter 查询 |
| 变点 | `ruptures.detection.Pelt` | PRD 指定；短序列降级阈值法 |
| CLI | Typer + Rich | 子命令清晰、终端摘要好看 |
| 配置 | `.git-arch.yml`（PyYAML） | 与 PRD 一致 |
| 缓存 | SQLite + 文件 blob | 以 blob hash 为 key，跨平台、可增量 |
| 报告 | Jinja2 模板 → `.md` / `.html` | 同一数据模型渲染双格式；HTML 内联 CSS/JS，单文件可分享 |

---

## 2. 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│  CLI (typer)                                                │
│  analyze | init | explain | hotspots | timeline             │
└─────────────┬───────────────────────────────────────────────┘
              │ Config (CLI > .git-arch.yml > defaults)
              ▼
┌─────────────────────────────────────────────────────────────┐
│  Pipeline Orchestrator                                      │
│  1 Scan → 2 Metrics → 3 Detect → 4 Attribute → 5 Report     │
└──┬──────┬──────────┬──────────┬────────────┬────────────────┘
   │      │          │          │            │
   ▼      ▼          ▼          ▼            ▼
 History  Metric   Changept  Attribution  Report
 Scanner  Engine   Detector  Engine       Renderer
   │      │          │          │         ├─ Markdown
   │      │          │          │         ├─ HTML
   │      │          │          │         └─ JSON (SARIF@M2)
   ▼      ▼          │          │
 Git     Cache       │          │
 Repo    (blob→AST/  │          │
         complexity) │          │
                     ▼          ▼
              Domain Models (HistoryGraph, TimeSeries,
                             DecayEvent, ArchaeologyReport)
```

### 2.1 模块划分（包结构）

```
git_arch/
  __init__.py
  __main__.py                 # python -m git_arch
  cli/
    app.py                    # Typer 入口
    analyze.py
    init_cmd.py
    explain.py
    hotspots.py
    timeline.py
  config/
    schema.py                 # dataclass / pydantic 配置模型
    loader.py                 # 合并默认值、yml、CLI
  git/
    repo.py                   # 定位仓库、分支、rev-list
    history.py                # HistoryGraph 构建（--follow / 存活血统）
    blame.py                  # blame 贡献者分布
    parse.py                  # commit message / 改动量解析
  metrics/
    base.py                   # Metric 协议
    complexity.py
    churn_complexity.py
    ownership.py
    registry.py               # --metrics 选择
    aggregate.py              # 文件 / 目录聚合
  detect/
    normalize.py              # z-score / min-max
    pelt.py
    threshold.py              # 短序列降级
    merge.py                  # 同 commit 多指标 → DecayEvent
    severity.py               # low|medium|high
  attribute/
    rules.py                  # ≥5 条病因规则
    templates.py              # 因果链自然语言模板
    anonymize.py              # Contributor#N
  report/
    model.py                  # ArchaeologyReport DTO
    markdown.py
    html.py                   # 单文件 HTML + 内联图表
    json_export.py
    suggestions.py            # 可行动建议生成
  cache/
    store.py                  # .git-arch/cache/ + SQLite
    keys.py                   # blob hash + tool_version + metric_cfg
  pipeline/
    analyze.py                # 编排与增量
  models.py                   # 跨模块共享类型
```

---

## 3. 核心数据模型

```python
@dataclass
class CommitRef:
    sha: str
    timestamp: datetime
    message: str
    parents: list[str]
    is_merge: bool

@dataclass
class FileRevision:
    path: str                 # 当前路径（follow 后）
    blob_hash: str
    commit: CommitRef
    lines_added: int
    lines_deleted: int

@dataclass
class HistoryGraph:
    """commit → 存活文件 → 版本映射；仅包含仍可达 HEAD 的血统。"""
    commits: list[CommitRef]          # 时间升序
    files: dict[str, list[FileRevision]]  # path → 沿血统的修订序列
    path_filter: str | None
    branch: str
    since: datetime | None
    until: datetime | None

@dataclass
class MetricPoint:
    commit_sha: str
    timestamp: datetime
    value: float
    extras: dict[str, Any]            # e.g. author_entropy, churn_count

@dataclass
class TimeSeries:
    entity_id: str                    # file path 或 module dir
    entity_kind: Literal["file", "module"]
    metric: str
    points: list[MetricPoint]

@dataclass
class ChangePoint:
    entity_id: str
    metric: str
    commit_sha: str
    timestamp: datetime
    before_value: float
    after_value: float
    delta: float
    confidence: float                 # 0~1
    severity: Literal["low", "medium", "high"]

@dataclass
class DecayEvent:
    """同 commit 上多指标突变合并后的「腐化事件」。"""
    commit_sha: str
    timestamp: datetime
    entity_ids: list[str]
    change_points: list[ChangePoint]
    hypothesis_id: str | None
    causal_chain: str
    severity: Literal["low", "medium", "high"]

@dataclass
class HotspotProfile:
    path: str
    current_metrics: dict[str, float]
    history: list[TimeSeries]
    events: list[DecayEvent]
    suggestions: list[str]

@dataclass
class ArchaeologyReport:
    meta: dict                        # 范围、耗时、版本、参数
    summary: dict                     # Top5 热点、Top3 拐点
    global_series: list[TimeSeries]
    events: list[DecayEvent]
    hotspots: list[HotspotProfile]
    actions: list[str]
    appendix: dict
```

中间产物落盘（可选，便于调试）：
- `.git-arch/work/history.json`
- `.git-arch/work/series.json`
- `.git-arch/work/events.json`
- 最终报告由用户 `--output` / `--format` 指定。

---

## 4. 流水线详细设计

### 4.1 F1 仓库扫描与历史遍历

**流程：**
1. `git rev-parse --show-toplevel` 定位仓库根。
2. 解析 `--branch`（默认 `HEAD`）、`--path`、`--since`/`--until`、`--max-commits`。
3. 用 `git log --reverse --pretty=... --name-status`（可加 `--follow` 按路径）收集 commit 列表。
4. **存活血统过滤（MVP 简化实现）**：
   - 取当前 HEAD 下目标路径的文件列表（尊重 ignore）。
   - 对每个文件：`git log --follow --pretty=%H -- <path>` 得到该文件仍关联到 HEAD 的 commit 序列。
   - 并集构成分析 commit 集合；已删除文件不进入热点（可在附录统计「历史删除噪音已忽略」）。
5. 构建 `HistoryGraph`；超过 `--max-commits` 时截断最早部分并在报告 meta 中注明。

**性能策略：**
- 批量 `git cat-file --batch` 取 blob（配合缓存）。
- 路径 ignore（`vendor/**` 等）在枚举阶段即排除。

### 4.2 F2 腐化指标计算

#### 4.2.1 复杂度增长 `complexity`

对每个 `FileRevision`：
1. 查缓存：`key = hash(blob_hash, metric="complexity", tool_version, lizard_opts)`。
2. 未命中则读 blob 文本 → `lizard.analyze_file`（或内存字符串 API）→ 取文件级 **圈复杂度之和**（或 max；默认 **sum**，配置可改）。
3. 写入缓存；形成按 commit 对齐的 `TimeSeries`。

语言：`.py` / `.js` / `.jsx` / `.ts` / `.tsx` / `.java`；其他扩展跳过并计数到 meta。

#### 4.2.2 churn × 复杂度 `churn_complexity`

对文件路径 `p` 在 commit `c_i`：

\[
\text{churn}(p, c_i) = \text{该文件在 } [c_0..c_i] \text{ 上的修改次数（有 diff 的 commit 数）}
\]

\[
\text{churn\_complexity}(p, c_i) = \text{churn}(p, c_i) \times \text{complexity}(p, c_i)
\]

可选窗口版（配置）：仅统计近 \(W\) 次提交内的 churn，避免单调递增掩盖近期恶化。

**默认**：累计 churn × 当前复杂度（与 PRD 表一致）；配置 `churn_window: null | int`。

#### 4.2.3 所有权碎片化 `ownership_fragmentation`

对文件在 commit `c_i` 的 blame 快照（可用 `git blame -l c_i -- path`，结果按作者聚合行数）：

设作者行数占比 \(p_k\)，\(n\) 为作者数：

**熵（默认主指标）：**

\[
H = -\sum_k p_k \log_2 p_k
\]

归一化：\( H / \log_2(\max(n, 2)) \) → \([0,1]\)。

**基尼系数（辅，写入 extras）：**

\[
G = \frac{\sum_i\sum_j |x_i - x_j|}{2n\sum_i x_i}
\]

时间序列主值用归一化熵；报告中可同时展示作者数。

**脱敏**：归因与报告层将 email/name 映射为 `Contributor#N`（稳定哈希按仓库盐），除非 `show_authors: true`。

#### 4.2.4 聚合

- **文件级**：如上。
- **模块级**：按目录（默认深度 2，可配）对子文件指标做加权平均（权=LOC 或等权）；变点检测对两级都跑，报告热点以文件为主、模块为辅。

#### 4.2.5 标准化（检测前）

对每条序列：

- 长度 ≥ 20：z-score（或配置 min-max）。
- 含常数序列：跳过变点，标记 `skipped_reason`。

### 4.3 F3 变点检测

1. `ruptures.Pelt(model="rbf", min_size=3).fit(signal).predict(pen=PENALTY[sensitivity])`
   - `low` / `medium` / `high` → 较大 / 中 / 较小 penalty（具体常数落地时用夹具仓库标定）。
2. 序列长度 `< 20`：降级为滑动窗口均值差阈值法（相对变化 > τ 且持续 k 点）。
3. 对每个变点索引 \(t\)：取 before = mean(window_left)，after = mean(window_right)，计算 `delta`、`confidence`（基于效应量 / 段内方差）。
4. `severity = |delta_norm| × metric_weight × confidence`，映射 low/medium/high。
5. **合并**：同一 `commit_sha`（允许 ±0 严格同 commit；可选配置 ±1）上多 `ChangePoint` → 一个 `DecayEvent`，severity 取 max。

**验收夹具**：内置/文档提供「重构后复杂度暴涨」合成仓库，要求检出 commit 在 ±1 内。

### 4.4 F4 归因与因果链

**规则库（MVP ≥ 5，按优先级匹配第一条为主假设，其余进附录）：**

| ID | 条件（可配阈值） | 假设文案要点 |
|---|---|---|
| R1 `giant_commit` | 该 commit 对该文件 `lines_added+del > N`（默认 500）且复杂度 delta 相对 > 50% | 巨型提交引入复杂度跃升 |
| R2 `ownership_burst` | ownership 指标变点且作者数 \(n_{after}-n_{before} \ge 2\) | 所有权碎片化 |
| R3 `hotfix_cluster` | 变点前后窗口内 fix/revert/hotfix message 占比 > ρ | 高频修复集中期 |
| R4 `churn_spike` | churn_complexity 变点且 churn 斜率突变 | 热点 churn 与复杂度叠加 |
| R5 `coupling_hint` |（M3 完整；MVP 简化）同 commit 共改模块数突增 | 隐式耦合形成迹象 |

循环依赖（PRD R2）MVP：**启发式**——同目录/跨包 import 边数量若可廉价解析则启用，否则标记 `unsupported` 不进主假设，避免假阳性。

**因果链模板示例：**

```text
{date} `{sha_short}`（{commit_summary}）导致 `{path}` 的 {metric_label}
从 {before} → {after}（Δ {delta_pct}%）。病因假设：{hypothesis}。
建议：{action_hint}
```

### 4.5 F5 报告生成（Markdown + HTML）

**同一 `ArchaeologyReport`，两套渲染器。**

#### 命令行为

```bash
git-arch analyze --format md --output ./archaeology-report.md
git-arch analyze --format html --output ./archaeology-report.html
git-arch analyze --format md,html --output ./archaeology-report
# 最后一种：写出 archaeology-report.md 与 archaeology-report.html
```

默认：`--format md,html`，输出目录当前目录，基名 `archaeology-report`（同时落盘两种文件），满足「除了 markdown 还要有 html」。

也可：

```bash
git-arch analyze --format both   # 别名 = md,html
```

#### 报告结构（两格式共用大纲）

0. 摘要：范围、commit 数、Top5 热点、Top3 拐点  
1. 腐化总览：全局曲线 + 指标说明  
2. 拐点时间线：事件、病因、因果链  
3. 热点文件档案：曲线、拐点、现状、建议  
4. 行动项  
5. 附录：算法参数、版本、缓存命中率  

#### Markdown 渲染
- Jinja2 → GFM。
- 曲线：Mermaid `xychart-beta` 或简化为表格 + sparkline 字符；兼容性不足时降级表格。
- 适合进 PR / Wiki。

#### HTML 渲染（M1 必做）
- **单文件**：CSS + 轻量 JS **全部内联**（可 `file://` 打开，无外网依赖）。
- 布局：页头摘要卡片 → 时间线 → 热点折叠面板 → 行动项。
- 图表：内联 **Chart.js**（vendor 进模板字符串或构建时嵌入）或纯 SVG 折线；优先 SVG 以减小依赖争议，数据点多时用 Chart.js。
- 交互（MVP）：拐点列表点击滚动到详情；严重度筛选（low/medium/high）；无需后端。
- 打印样式：`@media print` 隐藏筛选控件。

**建议动作生成（两格式共享 `suggestions.py`）：**
- 复杂度高 + churn 高 →「优先拆分 / 提取模块」
- 碎片化高 →「明确 code owner、缩小评审范围」
- hotfix 密集 →「补回归测试、冻结非必要改动」
- 仅历史尖刺已回落 →「观察，不必立即重构」

### 4.6 F6 缓存与增量

| 项 | 设计 |
|---|---|
| 位置 | `.git-arch/cache/`（`cache.sqlite` + 可选 `blobs/`） |
| Key | `blob_hash` + `analyzer_id` + `analyzer_version` + 配置指纹 |
| 值 | 复杂度标量、LOC、语言、耗时 |
| 失效 | 工具版本变更、指标相关配置变更 → 新命名空间或 bump `schema_version` |
| `--incremental` | 读取 `.git-arch/last_run.json` 的 `last_commit`；只追加新 commit；变点检测可对受影响文件全序列重跑（序列短） |
| `--no-cache` | 跳过读缓存，仍可写（或配置 `read_only_cache`） |

Blame 结果缓存：key = `(commit_sha, path, blob_hash)`，因 blame 更贵。

---

## 5. CLI 设计

```text
git-arch init
git-arch analyze [options]
git-arch explain <commit>
git-arch hotspots
git-arch timeline
```

### `analyze` 主要参数

| 参数 | 说明 |
|---|---|
| `--path` | 子路径 |
| `--since` / `--until` | 时间 |
| `--branch` | 分支 |
| `--max-commits` | 上限 |
| `--metrics` | `complexity,churn_complexity,ownership_fragmentation` |
| `--sensitivity` | `low\|medium\|high` |
| `--format` | `md` / `html` / `json` / `md,html` / `both` |
| `--output` | 路径或基名 |
| `--incremental` / `--no-cache` | 性能 |
| `--show-authors` | 关闭脱敏 |
| `--ci` / `--fail-on` / `--baseline` | M2，入口先保留并提示未实现或 stub |

终端摘要（Rich）：进度、拐点数量、Top 热点、写出的 **md 与 html 路径**。

---

## 6. 配置系统（M1 最小 + M2 完整）

M1：内置默认 + 可选 `.git-arch.yml` + CLI 覆盖。  
`git-arch init` 生成：

```yaml
metrics:
  complexity: { enabled: true, weight: 1.0, aggregate: sum }
  churn_complexity: { enabled: true, weight: 1.2, churn_window: null }
  ownership_fragmentation: { enabled: true, weight: 0.8 }
detection:
  algorithm: pelt
  sensitivity: medium
attribution:
  show_authors: false
  giant_commit_lines: 500
report:
  formats: [md, html]          # 默认双格式
  output: archaeology-report
ignore:
  - "vendor/**"
  - "**/*.min.js"
  - "**/node_modules/**"
cache:
  dir: .git-arch/cache
```

---

## 7. CI 与 SARIF（M2 预留）

- `--ci`：减少 emoji/动效，JSON 摘要打到 stdout，报告仍可写 md/html。
- 退出码：0 通过 / 1 超阈值 / 2 执行错误。
- `--format sarif`：把 `DecayEvent` 映射为 result；HTML/MD 不替代 SARIF。

---

## 8. 性能与资源预算

| 手段 | 说明 |
|---|---|
| Blob 级复杂度缓存 | 最大收益 |
| 存活血统 | 减少已删文件 |
| 并行 | `ProcessPoolExecutor` 按文件算复杂度（GIL 外） |
| Blame 抽样 | 可对无 ownership 变点需求的中间 commit 稀疏采样（配置 `blame_stride`） |
| 内存 | 流式写 series；避免一次性持有全部文件原文；目标 ≤ 2GB @ 10k commits |

---

## 9. 测试策略

| 层级 | 内容 |
|---|---|
| 单测 | 归一化、PELT 包装、短序列降级、规则匹配、脱敏、severity、报告 DTO |
| 黄金文件 | 固定迷你 git 仓库 fixture → 期望拐点 sha、报告片段 |
| 渲染快照 | MD/HTML 关键章节字符串快照 |
| 性能烟测 | 可选标记 `@slow` 合成 1k commits |

目标：检测 + 指标核心 **≥ 80%** 行覆盖。

---

## 10. 安全与隐私

- 不上传数据；关联 PR/issue 的网络拉取为 **opt-in**（M2+）。
- 默认作者脱敏；HTML 分享前需知报告可能含路径与 commit message。
- `.git-arch/` 建议加入项目 `.gitignore`（`init` 时提示）。

---

## 11. 里程碑与实现顺序（M1）

| 序号 | 任务 | 产出 |
|---|---|---|
| T1 | 工程脚手架：包、Typer、`init`、配置加载 | 可安装 CLI |
| T2 | Git HistoryGraph + ignore + max-commits | F1 |
| T3 | 缓存 + complexity 指标 | F2 部分 + F6 |
| T4 | churn_complexity + ownership | F2 完成 |
| T5 | PELT + 合并 + severity | F3 |
| T6 | 规则归因 + 脱敏 + 建议 | F4 |
| T7 | **Markdown + HTML** 报告渲染 | F5 |
| T8 | incremental 与终端摘要 | F6 闭环 |
| T9 | fixture 仓库 + 单测 + README | 可演示 MVP |

M2：配置增强、CI、baseline、SARIF。  
M3：隐式耦合、血统增强、HTML 图表升级。

---

## 12. 风险与缓解（工程向）

| 风险 | 缓解 |
|---|---|
| lizard 对 TS/复杂语法不准 | 文档标明局限；预留 tree-sitter 替换点 |
| PELT 误报 | sensitivity + confidence；事件合并 |
| Windows 路径 / 编码 | 统一 POSIX 路径入库；subprocess 文本 UTF-8 |
| HTML 单文件过大 | 热点默认 Top N；图表降采样 |
| 归因「政治化」 | 默认脱敏；文案强调系统与提交形态而非个人 |

---

## 13. 开放决策（已拍板）

1. **语言：Python**（非 Node）。  
2. **因果链：规则模板**（不做 LLM@MVP）。  
3. **报告受众：技术负责人**；`--audience exec` 留接口。  
4. **M1 报告格式：Markdown + HTML 均为默认交付**；JSON 可选。

---

## 14. 下一步建议

1. 按 §11 建仓库脚手架并实现 T1–T2。  
2. 同步补一份「假仓库」Markdown/HTML **报告样例**（便于评审报告形态）。  
3. 将三大指标伪代码抽成 `docs/metrics-spec.md`（若需与算法同事对齐）。
